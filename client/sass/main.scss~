@import "partials/variables";
@import "mixins/clearfix";

/*applying box-sizing property in order to stablish same-sized boxes*/
* {
  -webkit-box-sizing: border-box;
     -moz-box-sizing: border-box;
          box-sizing: border-box;
}

body {
    margin: 0;
    padding: 0;
    position: absolute;
    top: 0;
    bottom: 0;
    right: 0;
    left: 0;
}

/* text and headings */
h2 { 
  color: #393939; font-weight: bold; font-size: 28px; letter-spacing: -1px; 
}
h3 { 
  color: #393939; font-weight: bold; font-size: 16px; 
}

h4 { 
  color: #727272; font-size: 11px; text-transform: uppercase;
}

td {
    background-color:rgba(8,90,4,0.4);
}

/* Classes */
.full-background {
  /*background: url(../images/wow5.jpg) repeat center fixed; */
  -webkit-background-size: cover;
  -moz-background-size: cover;
  -o-background-size: cover;
  background-size: cover;

}

.container{
  margin: 0 auto;
  width: 80%;
  height: 100%;
  padding-left: 0px;
  padding-right: 0px;
}

.navbar-inverse{
  background-image: linear-gradient(to bottom, #366C5B 0px, #222 50%);
}

.table-striped>tbody>tr:nth-of-type(odd) {
    background-color: rgba(255, 255, 255, 0.46);
}

/* Media queries */
@media(min-width:767px) {
  .navbar {
      padding: 20px 0;
      -webkit-transition: background .5s ease-in-out,padding .5s ease-in-out;
      -moz-transition: background .5s ease-in-out,padding .5s ease-in-out;
      transition: background .5s ease-in-out,padding .5s ease-in-out;
  }

  .top-nav-collapse {
      padding: 0;
  }
}
